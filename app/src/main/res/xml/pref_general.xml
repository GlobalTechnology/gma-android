<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="ministry_team_list"
        android:title="@string/pref_title_ministry_team_text"
        android:defaultValue="0"
        android:entries="@array/pref_example_ministry_teams_demo"
        android:entryValues="@array/pref_example_ministry_teams_demo"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <!-- MCC = Mission Critical Component -->
    <ListPreference
        android:key="mcc_list"
        android:title="@string/pref_title_mcc"
        android:defaultValue="0"
        android:entries="@array/pref_mcc_demo"
        android:entryValues="@array/pref_mcc_demo"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
